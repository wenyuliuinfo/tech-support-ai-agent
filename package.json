{
  "name": "vibe-coding-starter-kit",
  "private": true,
  "scripts": {
    "dev": "concurrently --names web,api --prefix-colors blue,green \"pnpm dev:web\" \"pnpm dev:api\"",
    "dev:web": "pnpm --filter @vibe-coding-starter-kit/web dev",
    "dev:api": "cd src/api && uvicorn main:app --reload --port 8000",
    "build": "pnpm --filter @vibe-coding-starter-kit/web build",
    "lint": "pnpm --filter @vibe-coding-starter-kit/web lint",
    "lint:api": "cd src/api && .venv/bin/ruff check .",
    "test:api": "cd src/api && .venv/bin/python -m pytest",
    "check:structure": "cd src/api && .venv/bin/python -m pytest tests/test_structure.py -v",
    "typecheck:api": "cd src/api && .venv/bin/mypy --strict ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}